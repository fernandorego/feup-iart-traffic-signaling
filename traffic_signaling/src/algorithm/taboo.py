from traffic_signaling.src.model.schedule import Schedule
from traffic_signaling.src.model.city import City


def taboo_search(city: City, number_of_iterations: int) -> Schedule:
    return Schedule()
